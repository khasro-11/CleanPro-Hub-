# Database
DATABASE_URL="postgresql://cleanpro:cleanpro_dev_password@localhost:5432/cleanpro_hub"

# NextAuth v5
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-here-min-32-chars"
NEXTAUTH_URL="http://localhost:3000"

# Anthropic AI Agent
ANTHROPIC_API_KEY="sk-ant-..."

# Encryption key for sensitive fields (Steuer-ID, SV-Nr.)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY="your-32-byte-encryption-key-here"

# File uploads (local for dev, update for production)
UPLOAD_DIR="./public/uploads"
MAX_UPLOAD_SIZE_MB="5"
